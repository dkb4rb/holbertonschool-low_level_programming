#include "holberton.h"
/**
 * @brief 
 * 
 */

void print_numbers(void)
{
    int numbers = 48;
    while (numbers <= 57)
    {
        _putchar(numbers);
        numbers++;
    }
    _putchar('\n');
        
}