#include "holberton.h"
#include <stdio.h>

/**
* 
* 
*/

char *_strstr(char *haystack, char *needle)
{
    int i = 0;
    for (; i < haystack[i]; i++)
    {
    }

    if (haystack[i] == needle[i])
        return (needle);
    else
    {
        return ('\0');
    }
}