#include "holberton.h"
#include <stdio.h>

/**
* reset_to_98 - Reset value and updates the value it points to to 98.
*@n: pointer value
*/

int _strlen(char *s)
{
    int c = 0;
    while (*s != '\0')
    {
        c++;
    }

    return (c);
}
